*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Signing — never commit these to Git
keystore.properties
*.jks
*.keystore

# Internal planning and working notes. Kept on disk, out of the repo.
# CHECKLIST.md in particular carries pricing, revenue modelling and tax
# position, none of which belongs in a public repository.
BACKLOG.md
CHECKLIST.md
SUMMARY.md
design/
rules/

# Security review output, 2026-08-25. The patches are applied and the findings
# are closed; this directory is the working artefacts: the seven patch files,
# the emulator repro scripts, and the 765-line report. Kept on disk because the
# repro suite is worth re-running after any rules change, out of the repo
# because it documents holes in a public repository.
security-patches/

# Tooling output
graphify-out/
.kotlin/

# Agent configurations & instruction files
.agents/
CLAUDE.md
ARCHITECTURE.md
PROJECT.md
.claude/settings.local.json

# Generated knowledge graph — rebuild with `graphify update .`
graphify-out/

# System crash/error dumps
*.stackdump

# Firebase emulator logs, written into the repo root by `firebase emulators:start`
firebase-debug.log
firestore-debug.log
ui-debug.log

# Preserved source baseline and local implementation evidence
.source-baseline-20260912/
functions*/node_modules/
functions*/lib/
planning/
/app/google-services.json

# Local verification captures and UI automation output
.codex-tmp/

# Store-listing and submission working material
play-listing/
.devpost-hackathon-state.json
devpost-*.md
JUDGE-TESTING.md
NEXT-WEEK-TOGETHER-SPEC.md
PAMOJA-PRODUCT-AND-MONETIZATION.md
PRODUCT-ROADMAP-AND-JUDGE-ACCESS.md
RELEASE-AND-NEXT-FEATURES.md
REVENUECAT-AND-PLAY-SETUP.md

# One-off Firebase deployment configurations
firebase.*-deploy.json
firebase.release-fixes.json

# Release artifacts and symbol files
*.apk
*.aab
mapping.txt
native-debug-symbols.zip

# Local secrets and environment overrides
.env
.env.*
!.env.example
*.pem
*.p12
*.pfx
*.mobileprovision
