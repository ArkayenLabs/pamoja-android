<?xml version="1.0" encoding="utf-8"?><!--
   Android 12+ (android:dataExtractionRules). backup_rules.xml is the pre-12
   equivalent and the two must agree.

   Two separate channels, and allowBackup="false" would only have closed the
   first of them:

     cloud-backup     the copy that ends up in the user's Google account
     device-transfer  the copy that moves during set-up on a new phone

   Both are excluded for the same reason: the DataStore and the Firebase Auth
   token store are the only app data with any value, signing in re-derives all
   of it, and this is a health-adjacent app going into production. Until
   2026-08-25 this file was the untouched Android Studio template, which meant
   both channels carried everything.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="datastore" />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="datastore" />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
